comment: false
coverage:
  status:
    patch:
      default:
        target: 60%
    project:
      default:
        target: auto # target is the base commit coverage
        threshold: 10% # allow this little decrease on project
        base: auto
